{
  "scripts": {
    "dev": "dotenv -- turbo dev",
    "build": "dotenv -- turbo run build",
    "start:prod": "node apps/api/dist/main",
    "start:services": "docker-compose up -d",
    "docker:build": "docker build -t upload-archive .",
    "docker:run": "docker run -d -p 3000:80 upload-archive"
  },
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "turbo": "^1.10.16"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@nestjs/platform-ws": "^10.3.0",
    "chart.js": "^4.4.1"
  }
}
